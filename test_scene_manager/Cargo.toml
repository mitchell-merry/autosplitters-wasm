[package]
name = "test_scene_manager"
version = "0.1.0"
edition = "2021"

[dependencies]
asr = { path = "../../asr", features = ["alloc", "derive", "unity"] }
bytemuck = "1.23.1"
helpers = { path = "../helpers", features = ["unity"] }

[lib]
crate-type = ["cdylib"]
