[workspace]
resolver = "2"
members = [
    "cuphead",
    "dismantled",
    "doom_the_dark_ages",
    "helpers",
    "idtech",
    "kuru_kuru_kururin",
    "selaco",
    "snap_the_sentinel",
    "zdoom"
]

[build]
rustflags = ["-Aclippy::field_is_never_read"]

[workspace.dependencies]
asr = { git = "https://github.com/AlexKnauth/asr", branch = "mono-4" }
#asr = { path = "../asr" }
