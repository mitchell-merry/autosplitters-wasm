[package]
name = "katana_zero"
version = "0.1.0"
edition = "2021"

[dependencies]
asr = { path = "../../asr", features = ["alloc", "derive", "unity"] }
helpers = { path = "../helpers", features = ["unity"] }

[lib]
crate-type = ["cdylib"]
